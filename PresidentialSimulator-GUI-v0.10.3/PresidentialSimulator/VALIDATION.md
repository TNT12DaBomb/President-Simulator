# v0.10.3 validation

Java lint compilation and executable JAR build pass. Headless tests exercise all 13 desk routes and responsive layouts. Added regression checks that expired stat pulses stay at the base color and an ordinary campaign action is accepted while a decision is pending. Existing map, start and debate render fixtures run.

Native nonmodal-window focus, fullscreen and high-DPI behavior require physical desktop testing. Normal real-time and in-game deadlines remain in force. Save format unchanged.
